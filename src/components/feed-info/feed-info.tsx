import React, { FC, HTMLAttributes } from 'react';
import feedInfoStyles from './feed-info.module.css';

const FeedInfo: FC<HTMLAttributes<HTMLHtmlElement>> = () => {

    return (
        <main className={`${feedInfoStyles.main}`}>

        </main>
    );
};

export default FeedInfo;